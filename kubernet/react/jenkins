pipeline {
    agent any
    stages {
        stage('React Path') {
            steps {
                script {
                    dir('kubernet/react') {  // Change directory
                        sh 'kubectl apply -k .'  // Run kubectl within the directory
                    }
                }
            }
        }
    }
}
