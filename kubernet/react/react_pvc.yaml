apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: react-fast
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
